{
    "tasks": [
      {
        "type": "shell",
        "label": "gcc build active file",
        "command": "/usr/bin/gcc",
        "args": [
          "-fopenmp",
          "-g",
          "${fileDirname}/lockfreequeue.c",
          "${fileDirname}/main.c",
          "-o",
          "${fileDirname}/target/${fileBasenameNoExtension}"
        ],
        "options": {
          "cwd": "/usr/bin"
        }
      }
      
    ],
    "version": "2.0.0"
  }